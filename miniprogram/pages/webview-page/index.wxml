<view class="webview-page">
  <my-header title="{{title}}" bind:back="handleBack" />
  <view class="webview-page__top-gap" style="height: {{headerHeight}}px;"></view>
  <web-view wx:if="{{url}}" src="{{url}}" bindmessage="onWebviewMessage" />
</view>
